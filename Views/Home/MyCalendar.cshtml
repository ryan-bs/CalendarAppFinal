@{
    ViewData["Title"] = "My Calendar";
}

<script>
    document.addEventListener('DOMContentLoaded', function () {
        var calendarEl = document.getElementById('calendar');
        var calendar = new FullCalendar.Calendar(calendarEl, {
            initialView: 'dayGridMonth',
            events: @Html.Raw(ViewData["Events"]),
            resources: @Html.Raw(ViewData["Resources"]),
            headerToolbar: {
                left: 'today prev,next',
                center: 'title',
                right: 'timeGridWeek,dayGridMonth,listMonth'
            },
        });
        calendar.render();
    });
</script>

<div class="text-center">
    <h1 class="display-4">BEM VINDO AO SEU CALENDÁRIO!</h1>
    <div id='calendar'></div>
</div>

<div class="row">
    <div class="col-md-6">
        <a href="/Evento"><h3>Eventos</h3></a>
    </div>
    <div class="col-md-6">
        <a href="/Etiqueta"><h3>Etiquetas</h3></a>
    </div>
</div>